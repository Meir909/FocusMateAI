# 🚀 Развертывание FocusMate AI на Vercel

## 📋 Что нужно сделать:

### 1. Настройка Environment Variables на Vercel

1. Зайдите в ваш проект на Vercel
2. Перейдите в **Settings → Environment Variables**
3. Добавьте переменную:
   - **Name**: `GROQ_API_KEY`
   - **Value**: ваш Groq API ключ
   - **Environment**: Production, Preview, Development

### 2. Структура проекта

Убедитесь что у вас есть такая структура:
```
/
├── api/
│   ├── chat.js      # Serverless функция для чата
│   └── email.js     # Serverless функция для email
├── index.html       # Главная страница
├── chat.html        # Чат приложение
├── auth.html        # Страница входа
├── style.css        # Стили главной страницы
├── chat.css         # Стили чата
├── script.js        # JavaScript главной страницы
├── chat.js          # JavaScript чата
├── planner.js       # Планировщик
├── stats.js         # Статистика
├── notifications.js # Уведомления
├── profile.js       # Профиль
├── vercel.json      # Конфиг Vercel
└── package.json     # Зависимости
```

### 3. Развертывание

1. Закоммитьте все изменения:
```bash
git add .
git commit -m "Add Vercel serverless functions"
git push origin main
```

2. Vercel автоматически задеплоит проект

### 4. Проверка

После развертывания:
- Главная страница: `https://your-domain.vercel.app`
- Чат: `https://your-domain.vercel.app/chat.html`
- API endpoints: `https://your-domain.vercel.app/api/chat`

## 🔧 Если что-то не работает:

### Проверьте:
1. **Environment Variables** - добавлен ли `GROQ_API_KEY`
2. **CORS** - serverless функции должны иметь правильные заголовки
3. **Console errors** - откройте DevTools в браузере

### Тестирование API:
```bash
curl -X POST https://your-domain.vercel.app/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"Hello","mode":"friendly"}'
```

## 📱 Мобильная версия

Все мобильные оптимизации уже включены:
- Responsive design
- Touch-friendly кнопки
- Мобильная навигация
- Оптимизация для клавиатуры

## 🌐 Двуязычность

AI автоматически определяет язык пользователя:
- Если пишете на русском - отвечает на русском
- Если пишете на английском - отвечает на английском

---

**Готово!** 🎉 Ваш FocusMate AI теперь работает на Vercel!
